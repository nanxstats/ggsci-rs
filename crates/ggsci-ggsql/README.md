# ggsci-ggsql

Private scaffold for future ggsql integration.

This crate currently depends only on `ggsci` and provides small helpers that
emit explicit ggsql color arrays. A native dependency on `ggsql` is planned for
a later release.
